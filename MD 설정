# 에이전트 지침: 프로젝트 문서화 및 관리 규칙

당신은 자동화된 개발 보조 에이전트입니다. 작업 실행 시 **`/init-deep` 실행을 최우선 권장**하며, 아래 정의된 파일 강제 생성, `AGENTS.md` 컨텍스트 업데이트, 디렉터리 거버넌스 및 한국어 작성 규칙을 엄격히 준수해야 합니다.

---

## 0. 핵심 원칙 (Core Principles)

### 1) 모든 핵심 문서의 필수 생성 및 누락 방지 (Mandatory File Creation)
- 프로젝트 루트 디렉터리의 필수 문서 6종(`AGENTS.md`, `BACKLOG.md`, `CHANGELOG.md`, `README.md`, `SECURITY.md`, `SOLUTION.md`)은 **반드시 생성이 완료되어 존재해야 합니다.**
- 작업 시작 시 필수 파일 중 단 하나라도 누락되어 있다면, 작업을 진행하기 전 **즉시 프로젝트를 분석하여 누락된 마크다운 파일을 일괄 생성**해야 합니다.

### 2) AGENTS.md 실시간 맥락 동기화 (Strict Context Persistence)
- **에이전트는 모든 작업 수행 시 `AGENTS.md`를 최우선으로 읽고(Read), 변경 사항이 발생할 때마다 `AGENTS.md`에 실행 기록과 규칙을 추가(Write/Append)해야 합니다.**
- 누락 방지를 위해 에이전트는 자신이 수행한 분석 결과, 새로 정립된 개발 규칙, 프로젝트 특이사항을 반드시 `AGENTS.md`에 즉시 기록해야 합니다.

### 3) 실행 우선순위 및 트리거
- **최우선 권장 명령어**: 에이전트가 프로젝트에 처음 투입되거나 전체 초기화가 필요한 경우, 먼저 **`/init-deep`** (또는 `/init`) 명령어를 실행하도록 가이드합니다.
- **최초 실행 트리거**: 루트 디렉터리에 `BACKLOG.md` 또는 `AGENTS.md`가 없는 경우, 이를 최초 작업 상황으로 인식하여 전체 프로젝트 분석 후 필수 문서를 일괄 생성합니다.

### 4) 언어 작성 원칙 (필수)
- **기본 언어**: 생성하거나 수정하는 모든 문서, 변경 이력(Changelog), 작업목록(Backlog), 커밋 관련 설명, 마크다운 콘텐츠는 **무조건 한국어**로 작성해야 합니다.
- 코드 블록, 변수명, 터미널 명령어 및 일반적으로 쓰이는 기술 용어(예: Semantic Versioning, Pre-Push)는 영문 그대로 표기할 수 있습니다.

### 5) PowerShell 실행 인코딩 원칙 (필수)
- **PowerShell 명령어 실행 시 반드시 UTF-8 인코딩으로 실행해야 합니다.**
- CLI 명령어 및 스크립트 실행 시 한글 깨짐이나 문자 인코딩 오류가 발생하지 않도록 UTF-8 인코딩 설정을 강제해야 합니다.

---

## 1. 명령어 트리거 및 초기화 규칙

### `/init-deep` (권장) 또는 `/init`
1. 프로젝트 아키텍처, 기존 코드베이스, 기술 스택을 깊이 있게 분석합니다.
2. 필수 마크다운 파일 6종이 없다면 일괄 생성합니다.
3. `AGENTS.md`에 **프로젝트 요약, 빌드/테스트 방법, 에이전트 행동 지침, 서브 시스템 구조**를 상세히 기록합니다.

### 최초 실행 검사 (`BACKLOG.md` 또는 `AGENTS.md` 부재 시)
- 필수 파일이 존재하지 않는 경우 최초 실행으로 판단하고, 전체 프로젝트 분석 후 `BACKLOG.md`(개선 과제 목록)와 `AGENTS.md`(시스템 지침)를 한국어로 동시 생성합니다.

---

## 2. 루트 디렉터리 거버넌스 정책

프로젝트 루트 디렉터리에는 **오직 아래 6개의 마크다운 파일만 허용**되며, 6개 파일은 **모두 반드시 존재**해야 합니다:
- `AGENTS.md`
- `BACKLOG.md`
- `CHANGELOG.md`
- `README.md`
- `SECURITY.md`
- `SOLUTION.md`

**이행 규칙:**
- 루트 디렉터리에 존재하는 위 6개 이외의 모든 `.md` 파일은 예외 없이 `docs/` 디렉터리로 이동시켜 관리합니다.
- 위 6개 파일 중 존재하지 않는 파일이 있다면 작업을 진행하지 말고 즉시 기본 내용이 채워진 파일로 생성합니다.

---

## 3. 마크다운 파일별 역할 및 업데이트 규칙

### 1. `AGENTS.md` (에이전트 지식 및 컨텍스트 관리 - 필수 지속 업데이트)
- 에이전트 설정, 프로젝트 구성 요소, 에이전트가 파악한 핵심 컨텍스트를 유지합니다.
- **업데이트 규칙**: 새로운 패키지 추가, 아키텍처 변경, 주요 버그 수정 방식을 알게 될 때마다 `AGENTS.md`에 지식 항목을 지속적으로 누적 기록합니다.

### 2. `SOLUTION.md` (문제 해결 및 지식 베이스)
- 개발, 테스트, 디버깅 과정에서 알게 된 지식과 트러블슈팅 경험을 한국어로 기록하여 동일한 실수를 반복하지 않도록 합니다.
- 다른 `.md` 파일에 존재하는 문제 해결 내용을 수집하여 `SOLUTION.md`로 이관·통합합니다.

### 3. `CHANGELOG.md` (버전 및 변경사항 추적)
- 코드 변경 사항을 분석하여 상세 내역을 한국어로 정리합니다.
- 변경 범위를 평가하여 시맨틱 버저닝(`MAJOR.MINOR.PATCH`) 기준에 맞추어 버전을 업데이트합니다.

### 4. `BACKLOG.md` (할 일 관리)
- 프로젝트 TODO 항목을 **H (High/높음)**, **M (Medium/중간)**, **L (Low/낮음)** 그룹으로 분류합니다.
- 각 TODO 항목에는 고유 라벨(예: `H1`, `H2`, `M1`, `L1`)을 부여합니다.
- 작성 포맷 예시:
  ```markdown
  ## 높음 (H)
  - [ ] [H1] 높은 우선순위 작업 설명
  ## 중간 (M)
  - [ ] [M1] 중간 우선순위 작업 설명
  ## 낮음 (L)
  - [ ] [L1] 낮은 우선순위 작업 설명
